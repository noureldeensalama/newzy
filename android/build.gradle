//allprojects {
//    repositories {
//        google()
//        mavenCentral()
//    }
//}
//
//rootProject.buildDir = "../build"
//subprojects {
//    project.buildDir = "${rootProject.buildDir}/${project.name}"
//}
//subprojects {
//    project.evaluationDependsOn(":app")
//}
//
//tasks.register("clean", Delete) {
//    delete rootProject.buildDir
//}
plugins {
    // ...
//    id 'com.android.tools.build:gradle:8.1.4' // Ensure this is compatible with your Flutter version

    // Add the dependency for the Google services Gradle plugin
//    id 'com.google.gms.google-services' version '4.3.15' apply false

}
allprojects {
    repositories {
        google()
        mavenCentral()
    }
//    dependencies {
////        classpath 'com.android.tools.build:gradle:8.1.4' // Ensure this is compatible with your Flutter version
//        classpath 'com.google.gms:google-services:4.4.2' // Firebase plugin
//    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"

}
subprojects {
    project.evaluationDependsOn(":app")

}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
